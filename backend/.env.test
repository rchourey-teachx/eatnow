# Test Environment
PORT=3001
NODE_ENV=test

# PostgreSQL (use same as dev for integration tests)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=fooddelivery
POSTGRES_PASSWORD=fooddelivery123
POSTGRES_DB=fooddelivery

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=food-delivery-test
KAFKA_GROUP_ID=food-delivery-test-group
